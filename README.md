# 해피트리 영어학원 진도 관리 시스템

## 최근 변경 이력

---

### v3.3 — 2025-05-16 (교재 학습 관리 예외 기능 버그 수정 + UI 개선)

#### 🐛 버그 수정 1: 예외 설정 — 반+교재 간 학생 데이터 독립성 문제

**문제**
- A반에서 AA교재에 "abc" 학생을 예외 등록한 뒤, 동일 A반의 BB교재에도 "abc" 학생을 등록하면 **AA교재에 등록된 "abc"의 예외 설정이 사라지는 버그**
- 원인: 예외 데이터를 `classId` 단위로 저장할 때 학생 이름이 flat key로 사용되어, 같은 이름의 학생이 여러 교재에 존재할 경우 덮어쓰기 발생

**수정 내용 (`booklib-app.js`)**

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 저장 구조 | `{ 학생명: { bookId, items, ... } }` | `{ bookId: { 학생명: { items, ... } } }` |
| 저장 위치 | `hakwon10/exempts/{classId}` | 동일 (구조만 변경) |
| 교재 간 독립성 | ❌ 같은 이름 학생이 서로 덮어씀 | ✅ bookId를 1차 키로 완전 분리 |

**하위 호환성**: 기존 구버전 데이터 자동 마이그레이션 (`_migrateExemptsIfNeeded` 헬퍼 추가)

**영향 함수**
- `_migrateExemptsIfNeeded()` — 신규 추가, 구버전 데이터 자동 변환
- `_saveExemptMgr()` — 신구조로 저장 (해당 bookId 항목만 교체, 다른 교재 보존)
- `_loadExemptMgrList()` — bookId 기준 필터링으로 변경
- `_deleteExemptItem()` — 파라미터에 `bkId` 추가, 신구조 기반 삭제
- `_runBatchImport()` 내 exempts 로드 — 신구조 기반으로 수정
- `openCsvImportModal()` 내 exempts 로드 — bookId 필터 적용

---

#### ✨ 기능 개선 2: 예외목록 — 반별 슬라이더 UI

**문제**
- 모든 반/교재의 예외 항목이 단순 목록으로 나열되어 항목이 많을수록 찾기 불편

**수정 내용 (`booklib-app.js` — `openExemptList`)**

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| UI 구조 | 단순 세로 목록 | 반별 슬라이더 (좌/우 이동) |
| 반 이동 | 없음 | 이전/다음 버튼 + 퀵점프 버튼 |
| 교재 구분 | 반+교재 섹션 구분 | 슬라이드 안에 교재별 섹션 |
| 삭제 | 전체 반 통합 삭제 | 슬라이드 내 개별 삭제 |
| 수정 이동 | 수정 버튼 → 예외 관리로 이동 | 교재별 수정 버튼 유지 |

**UI 구성**
```
┌─────────────────────────────────────────────┐
│  📋 예외 학생 전체 목록             [✕]      │
│  QUICK → [A반] [B반] [C반]  ← 퀵점프       │
├─────────────────────────────────────────────┤
│  ‹ 이전     A반  (1 / 3)     다음 ›        │
│                                              │
│  📖 AA교재  (2명)               [✏️ 수정]  │
│  ┌────────────────────────────────────────┐ │
│  │ 홍길동  면제: 암기 리콜          [🗑] │ │
│  │ 김철수  면제 항목 없음           [🗑] │ │
│  └────────────────────────────────────────┘ │
│  [＋ 예외 학생 추가]                         │
│                                              │
│  📖 BB교재  (1명)               [✏️ 수정]  │
│  ┌────────────────────────────────────────┐ │
│  │ 홍길동  면제: 테스트             [🗑] │ │
│  └────────────────────────────────────────┘ │
│  [＋ 예외 학생 추가]                         │
└─────────────────────────────────────────────┘
```

---

## 파일 구성

| 파일 | 역할 | 버전 |
|---|---|---|
| `index.html` | 앱 진입점 | — |
| `app.js` | 메인 앱 (진도 관리) | v10 |
| `booklib-app.js` | 교재 학습 관리 UI | **v3.3** |
| `booklib-db.js` | 교재 학습 데이터 | v3.1 |
| `grade-app.js` | 성적 관리 UI | — |
| `grade-db.js` | 성적 데이터 | v2.0 |
| `students-app.js` | 학생 관리 UI | — |
| `students-db.js` | 학생 데이터 | v1.0 |
| `staff-app.js` | 직원 관리 UI | — |
| `staff-db.js` | 직원 데이터 | v2.0 |
| `db.js` | 공통 DB | — |
| `firebase-config.js` | Firebase 설정 | v6 |
| `style.css` | 전역 스타일 | — |
| `logo.js` | 로고 | — |

## Firebase 데이터 경로

```
hakwon10/
  classes/          — 반 목록
  progress/         — 진도 체크
  accounts/         — 사용자 계정
  theme/            — 테마 설정
  booklib/          — 교재 목록
  bookcheck/        — 학습 체크 현황
  bookstamps/       — 진도 스탬프
  stampInfo/        — 스탬프 정보
  memos/            — 교재별 메모
  exempts/{classId} — 예외 설정 (v3.3: { bookId: { studentName: {...} } })
  grades/           — 성적 데이터
  students/         — 학생 정보
  staff/            — 직원 정보
  staffwork/        — 직원 근무 기록
  stafftempl/       — 직원 주간 템플릿
```

## 배포

GitHub Pages 자동 배포: [https://kyuhwajeong.github.io/htdev/](https://kyuhwajeong.github.io/htdev/)
